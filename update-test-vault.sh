#!/bin/bash

echo "Test vault is already set up with symlinks to main.js and manifest.json."
echo "These files will automatically reflect any changes after rebuilding the plugin."
echo "To test the plugin, open Obsidian and select the test-vault directory." 